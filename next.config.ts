import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['k.kakaocdn.net', 'example.com', 'images.pexels.com', 'images.unsplash.com', 'i.ibb.co', 'nemo-uploaded-files.s3.ap-northeast-2.amazonaws.com'],
  },
};

export default nextConfig;
