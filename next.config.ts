import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['k.kakaocdn.net', 'example.com', 'images.pexels.com', 'images.unsplash.com', 'i.ibb.co'],
  },
};

export default nextConfig;
